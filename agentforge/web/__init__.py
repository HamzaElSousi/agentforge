"""Optional web dashboard for AgentForge.

A small FastAPI app that runs a pipeline and streams its events live to the
browser, so you can pick a pipeline, type a goal, hit Run, and watch the agents,
tool calls, and cost update in real time — no commands to remember.

Requires the ``web`` extra: ``pip install "agentforge[web]"``.
"""
